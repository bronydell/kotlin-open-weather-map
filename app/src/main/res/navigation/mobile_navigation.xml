<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/mobile_navigation"
        app:startDestination="@id/nav_home">

    <fragment
            android:id="@+id/nav_home"
            android:name="com.danielvilha.kotlinopenweathermap.WeatherFragment"
            android:label="@string/app_name"
            tools:layout="@layout/fragment_weather" />
    <fragment
            android:id="@+id/nav_select_city"
            android:name="com.danielvilha.kotlinopenweathermap.SelectCityFragment"
            android:label="@string/select_city"
            tools:layout="@layout/fragment_select_city" />
</navigation>